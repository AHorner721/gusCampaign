<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!--Import Google Icon Font-->
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- google fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@1,800&display=swap" rel="stylesheet">
    <!-- Compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="/css/main.css">
    <title>Donate</title>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col s12 l12">
                <form id="card" action="/charge" method="POST">
                    <h5 class="center">Complete Your Donation</h5>
                    <p class="center">Thank you, <%= name %>, for your generosity! Please enter your payment details below to complete your donation.</p>
                    <div class="input-field">
                        <input type="text" id="cardholder-name" name="cardholder-name" value=<%= name%> required>
                        <label type="text" for="cardholder-name">Cardholder-name:</label>
                    </div>
                    <div class="input-field">
                        <input type="text" id="address" name="address" required>
                        <label type="text" for="address">Street Address:</label>
                    </div>
                    <div class="stripeInput">
                        <label for="card-element">Credit or Debit Card Number:</label>
                        <div id="card-element"></div>
                        <hr>
                    </div>
                    <div class="error" id="card-errors"></div>
                    <div class="input-field center">
                        <button id="card-button" class="btn red darken-4" data-secret=<%= intentSecret %>>Donate $<%= amount %></button>
                    </div> 
                </form>
            </div>
        </div>
    </div>
    <!-- stripe element handling -->
    <script src="https://js.stripe.com/v3/"></script>
    <script type="text/javascript" src="/stripe.js"></script>
    <!-- Compiled and minified Materialize Lib -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js"></script>
</body>
</html>